# beaver-guardian-google (archive — scheduled for deletion)

This repo was a build/testing scratch space for the SafeGuard web prototype (APK scans, build zips, a local `node_modules/` checkout). It's not an active project and shouldn't be treated as one.

The real projects are:
- [beaver-guardian](https://github.com/tareknahas85-star/beaver-guardian) — the actual working parental-control Android app (Kotlin, native)
- [beaver-guardian-safeguard-webapp](https://github.com/tareknahas85-star/beaver-guardian-safeguard-webapp) — the active web dashboard prototype

This repo still has a leftover `node_modules/` folder committed to git (a mistake — a `.gitignore` was missing when it was first pushed). It's kept as-is here rather than partially cleaned up.

**Recommended:** delete this repository from GitHub settings (Settings → General → Danger Zone) rather than keep maintaining it. Nothing here is unique — it's all duplicated or superseded by the two repos above.
